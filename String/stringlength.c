#include <stdio.h>
int main()
{
    char s[100];
    int i;
    printf("enter string:");
    scanf("%s", s);
    for (int i = 0; s[i] != '\0'; i++)
        
    printf("total=%d\n", i);
    return 0;
}